# React/TypeScript frontend, Python/FastAPI backend

The hour rules have to run in both the browser, so the weekly grid can flag a day as
the employee types, and again on the server, so the API stays safe for the
mobile and genAI clients the product expects to grow. We chose React +
TypeScript + Vite on the frontend and Python + FastAPI on the backend
